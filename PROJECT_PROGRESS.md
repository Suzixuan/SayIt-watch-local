# SayIt Watch Transport project progress

Overall: Delivery 1A accepted. Delivery 1B Provider contract is MATCH after PM acceptance of Z2; the PC-only external WAV ingress slice is unlocked. Delivery 1B real-device closure is not yet complete.

| Group | # | Task | Acceptance | Status | Owner | Dependency / evidence |
|---|---:|---|:---:|---|---|---|
| Foundation | 1 | Isolated private baseline and PM authority files | ☑ | 🟢 Completed | PM | Private `main` baseline import `18400ad82d7f5a7009a47622248643022472650f` |
| Delivery 1A | 2 | Watch debug recorder and WAV writer | ☑ | 🟢 Accepted | PM, Colleague D | PM reviewed editable Wear fields and live sample-derived duration; Android 45/45 tests, lint, Debug and Release builds independently passed |
| Delivery 1A | 3 | Debug-only Windows LAN receiver | ☑ | 🟢 Accepted | PM, Colleague D | PM reviewed 64-hex token, request-ID preservation and strict RIFF bounds; Rust 141 passed / 0 failed / 4 ignored |
| Delivery 1A | 4 | Android and Windows source/build verification | ☑ | 🟢 Accepted | PM | Fresh PM runs: Android 45/45 plus lint/Debug/Release builds; Vitest 339/339 and client build; Rust 141/141 non-ignored tests; release receiver-marker scan clean |
| Delivery 1A | 5 | Galaxy Watch 7 transport and manual WAV playback gate | ☑ | 🟢 Accepted | User, PM | PM independently parsed 14.88 s / 238,080-sample 16 kHz s16le mono WAV and matched SHA-256; user confirmed human playback acceptance on 2026-08-29 |
| Delivery 1B | 6 | Freeze existing Provider input contract | ☑ | 🟢 Accepted — MATCH | PM, Colleague Z | Revision 3 `c89c0bc` plus PM-accepted Z2 runtime proof; frozen contract in `docs/DELIVERY-1B-PROVIDER-CONTRACT.md` |
| Delivery 1B | 6A | Prove ≥9 MiB raw Tauri binary IPC in packaged Windows WebView2 | ☑ | 🟢 PM accepted | PM, Colleague Z | Z commit `d1f7c1fa`; PM fresh native run: identical 9,438,418-byte `ArrayBuffer`, SHA-256 `ebf783be…45750`, sentinels 7/190/86, fallback warnings 0; PM rebuild exe SHA-256 `0fdf6bb7…219f` |
| Delivery 1B | 7 | External WAV ingress through existing History/Paste | ☐ | 🟠 PC Repair accepted; Watch UI Repair 2 required | Colleague Z | PM retained the three PC-side repairs. Watch UI is NO-GO: Cancel late-completion race can end in Failure; Later makes Retry unreachable; runtime 52 dp circular Buttons do not match the pill-button previews; baseline hashes are not the parent blobs. Task: `docs/DELIVERY-1B-Z3-REPAIR-2.md` |
| Acceptance | 8 | Ten consecutive real end-to-end runs | ☐ | ⚪ Locked | User, PM | Record five stages, Stop-to-Paste latency, median and P95 |

Current external slice: Delivery 1B Z3 Repair 2 — Watch UI behavior/evidence correction, assigned to colleague Z via `docs/DELIVERY-1B-Z3-REPAIR-2.md`. The three PC-side Repair 1 blockers are PM-retained and must not be rewritten. Not VERIFIED end-to-end; real Galaxy Watch visual/interaction closure and the ten-run acceptance remain locked.

Updated sequencing decision (user, 2026-08-29): colleague Z will do the Watch UI in the same task package as Z3 Repair 1, but only after the three Repair blockers pass automated verification. First real Galaxy Watch → existing SayIt → focused Windows input-box closure remains a later PM-unlocked device gate. The formal ten-run latency acceptance remains final.

Z1 PM review (2026-08-29): scope control passed (only the three allowed documentation files changed), but the contract decision is NO-GO. The submitted design omits the required per-run Provider `connect(callbacks)` and explicit successful `start` before PCM, treats asynchronous native context capture as synchronous, leaks the JS reservation on Rust-side aborts, uses an unsafe 30-second JS watchdog, permits a lost post-201 handoff, and does not freeze the audio accounting required by History/timeouts. Repair instructions: `docs/DELIVERY-1B-Z-CONTRACT-REPAIR-1.md`.

PM review decision (2026-08-29): **NO-GO** on the original submission. Repair instructions were frozen in `docs/DELIVERY-1A-D-REPAIR-1.md`; all six items are now repaired and pushed.

Repair 1 verification (Colleague D, 2026-08-29, branch `codex/review-watch-transport`):

- `watch`: `.\\gradlew.bat testDebugUnitTest lintDebug assembleDebug` — BUILD SUCCESSFUL (exit 0), 45 unit tests / 0 failures (added DevTokenValidator tests: 63/65 chars, non-hex, whitespace, valid 64; TransportStatus requestId mismatch/missing tests), lint clean.
- `client`: `npm test` — 29 files / 339 tests passed; `npm run build` — exit 0.
- `client/src-tauri`: `cargo test` — 145 tests, 141 passed / 0 failed / 4 ignored (exit 0); added receiver X-Request-Id (missing/invalid -> 400, echo on 201) and strict RIFF extent tests.
- Release safety: `cargo build --release` binary contains none of `watch-receiver`, `SAYIT_WATCH_BIND_IP`, `received_watch.wav`, `X-Request-Id`, `api/watch/audio`; Watch release APK manifest denies cleartext, debug APK permits it.
- `rust-toolchain.toml` restored to baseline `channel = "stable-x86_64-pc-windows-msvc"`; builds/tests verified under it (earlier failure was missing MSVC toolchain, now installed via rustup).
- Debug APK (rebuilt): SHA-256 `1275C49D508A8E03AFD7E9D84F7EC668BE12332DE3E479152DDA38C20873E8D6` (path `watch/app/build/outputs/apk/debug/app-debug.apk`, not committed).
- Repair-1 real-device evidence: three settings entered via real Wear keyboard (IP `192.168.12.142`, port `18099`, 64-hex token) with screenshots; live `Recording 3520 ms` observed mid-capture; new recording uploaded with valid X-Request-Id — receiver logged `requestId=bb327d88-96a3-4af5-b700-c7eeeaeee942 bytes=476204 samples=238080 durationMs=14880`, saved `received_watch.wav` SHA-256 `2929585a641a1f6d55d3aa715d8f21d153f67941742c5eb299fa135cd3de02d2`; desktop copy `received_watch_D-repair-voice.wav` for PM playback.

PM acceptance evidence (2026-08-29): Android unit tests rerun with `--rerun-tasks` (45 passed, 0 failed), lint plus Debug/Release APK builds succeeded; client Vitest 339 passed and production build succeeded; Rust tests reported 141 passed, 0 failed, 4 ignored; Rust release build succeeded and the five debug-receiver markers were absent. The repair APK and received Watch WAV hashes were independently checked. The received repair WAV parses as PCM s16le, 16 kHz, mono, 14.88 seconds and 238,080 samples. Delivery 1A is accepted. Stop here until Delivery 1B is explicitly dispatched.

Z1 submission (Colleague Z, 2026-08-29, branch `codex/review-watch-pipeline`, base `5cc14b4c8658920cc9ca9b21ab1dcaf878f1a136`) — **superseded by Repair 1, retained for audit**: submitted `docs/DELIVERY-1B-PROVIDER-CONTRACT.md` with verdict MATCH. The audio-clause confirmations (`audio.ts` 16 kHz/mono/Int16 `bytes/2/16000`; `wav.rs` PCM/mono/16 kHz/16-bit) and the `aiEnabled`/`disableAi` per-run freeze remain valid; the lifecycle order claim, the synchronous-capture claim, the single-side release design, the 30 s watchdog, and the unconditional MATCH were corrected in revision 2.

Z1 contract revision 2 (Colleague Z, 2026-08-29, branch `codex/review-watch-pipeline`, review base `8240d4a2a59de96608f1ec907fe50ba5f400467b`): `docs/DELIVERY-1B-PROVIDER-CONTRACT.md` rewritten per `docs/DELIVERY-1B-Z-CONTRACT-REPAIR-1.md`, awaiting PM re-review. Verdict **CONDITIONAL MATCH** — the one condition is the Tauri v2 raw binary-IPC spike, frozen as the first stop/go gate of the implementation slice (pinned `tauri 2.10.3` / `@tauri-apps/api 2.10.1` source evidence recorded in §B.4; base64 fallback for the full 10 MiB body not authorized). Corrections: (1) true per-run lifecycle `isReady` pre-flight → `connect(buildProviderCallbacks())` → checked `start()` → `sendAudio*` → `stop()`, external run performs both indispensable steps with no PCM before start success and per-stage rollback; (2) two-phase reservation — Phase A fully synchronous (checks + runId + reservation + the genuinely synchronous `captureActiveInsertionTarget`), Phase B the bounded (3 s) asynchronous native `getRecordingContext` capture whose completion gates the admission response, capture failure → `500` fail-closed; (3) one correlated abort operation with idempotent requestId+runId-conditional halves covering all nine trigger cases on both bridge sides; (4) the 30 s JS watchdog removed, replaced by a Rust-owned lazy lease (`300 s` = frozen `MAX_RECORDING_SEC` vs ≈164 s derived worst case) plus boot-time `watch_gate_state()` reconciliation for WebView reload; (5) `201` only after a bounded post-save acknowledgement (`watch_run_started`) proving PCM validated + callbacks connected + `start` succeeded, failures answer non-2xx and abort both sides; (6) external audio accounting frozen (`recordedChunks`, `audioSentSamples`, sample-derived `wallTimeAtStopSec`, timeout input, context fields; no mic-only `stopCapture()`; shared `finalizeRecording` helper extracted from `stopRecording`'s tail); (7) verdict downgraded to CONDITIONAL MATCH with the spike gate. All five task stop-conditions remain not triggered; only the three allowed documentation files changed.

Z1 PM re-review (2026-08-29): scope control and the seven Repair 1 architecture corrections passed, but §B.5 step 7 specifies `audioSentSamples += 4096` even for a final short chunk. This can overcount duration and contaminate minimum-duration, timeout, History and latency evidence. Decision: Repair 2 required; exact per-slice `chunk.byteLength / 2` accounting, end-of-feed equality checks and a non-divisible-by-4096 test contract are frozen in `docs/DELIVERY-1B-Z-CONTRACT-REPAIR-2.md`. Product implementation remains locked.

Z1 contract revision 3 (Colleague Z, 2026-08-29, branch `codex/review-watch-pipeline`, review base `dd7efb233f3ab85a85c740cecbfe644078cc7827`): `docs/DELIVERY-1B-PROVIDER-CONTRACT.md` narrowed to exactly the Repair 2 correction, awaiting PM re-review. Verdict unchanged at **CONDITIONAL MATCH**. Exact accounting frozen: chunks are even-length raw-PCM byte slices of a configured target (4096 samples recommended; final slice = exact remainder, even by construction); per chunk `chunkSamples = chunk.byteLength / 2` on the exact slice, the exact copied chunk is appended to `recordedChunks`, and `audioSentSamples += chunkSamples` — never the configured maximum; after the feed and **before** `provider.stop`, assertions require `audioSentSamples === admission.sampleCount` and `Σ recordedChunks byteLength === pcm.byteLength`, with any mismatch taking the correlated abort path (fixed non-secret reason `sample_accounting_mismatch`); `wallTimeAtStopSec`, `pttHoldMs`, the processing-timeout input, and History duration are derived **only** from the exact final `audioSentSamples`. §E row 7 adds the explicit non-divisible-by-target test case (final short chunk counted exactly; `recordedChunks` concatenation reproduces the admitted PCM byte-for-byte) plus a mismatch-abort case. Changed sections: §B.4, §B.5 steps 7-9, §C.1, §E row 7, §G (new R2 row), header/revision notes. All other Repair 1 decisions of revision 2 carry over verbatim — per-run lifecycle (§A.2), two-phase reservation (§B.2), correlated abort matrix (§B.6), Rust-owned lease and reload recovery (§B.7), post-save acknowledgement before `201` (§B.3), shared `finalizeRecording`/no-`stopCapture` rule, probe-once rule, and the binary-IPC spike gate (§B.4). `docs/WATCH-UI-Z-HANDOFF.md` treated as PM reference only; no UI work performed. Only the three allowed documentation files changed.

Z1 PM acceptance (2026-08-29): revision 3 commit `c89c0bc36bccc32281ca3158e1f111511467d39f` accepted as **CONDITIONAL MATCH**. PM independently confirmed the allowed three-file scope, clean diff, exact per-slice accounting, both pre-stop equality assertions, correlated mismatch abort, exact duration derivation, explicit short-final-chunk/mismatch test contract, and preservation of all Repair 1 decisions. Stage 6 is complete as a contract gate. The only unlocked condition is stage 6A, the isolated packaged-Windows raw binary-IPC spike; external ingress remains locked.

Watch UI review (2026-08-29): the corrected direction is frozen in `docs/WATCH-UI-Z-HANDOFF.md` as a future-slice reference. Key corrections: call it a Wear OS app rather than a watch face; model three screens plus inline upload states; mask the 64-hex Dev Token; define Cancel/Later and retained-WAV behavior; keep the activity awake programmatically during foreground recording/upload; and use transport-only success wording. This does not authorize UI code during the binary-IPC spike.

Z2 binary-IPC spike (Colleague Z, 2026-08-29, stage 6A): **PASS** in the isolated packaged Windows debug WebView2 harness at `spikes/watch-binary-ipc` (not SayIt product code; two reproducible runs, 126 ms / 113 ms invoke time). Rust command returned `tauri::ipc::Response::new(Vec<u8>)` with a deterministic 9,438,418-byte payload (9 MiB + 1234, even length, deliberately not an 8192-byte/4096-sample chunk multiple); `@tauri-apps/api` `invoke` received `[object ArrayBuffer]` with `byteLength` exactly 9,438,418; SHA-256 expected === actual `ebf783be13a56bd212a474803c6b8d6da391b10bc0cfc5721f9422c90c045750` (WebCrypto subtle over the raw buffer — no base64, no JSON number array); sentinels first/middle/last 7/190/86 matched Rust vs JS plus independent formula re-derivation; `console.warn` was wrapped programmatically across the whole invoke window and restored on all paths — **fallback-warning count 0, total warnings 0; `customProtocolIpcFailed` did not appear**. Commands and exit codes: `npm install` 0; `npm test` (vitest 8/8) 0; `npm run build` 0; `cargo test` (payload/hash unit tests 4/4) 0; `npx tauri build --debug --no-bundle` 0. Packaged debug executable `spikes/watch-binary-ipc/src-tauri/target/debug/spike-binary-ipc.exe`, SHA-256 `940a58476ac390998b352a73aa4a741f97c08866a799002685d2020e02c0abf7` (not committed). Panel screenshot retained session-locally outside the repository; payload never logged or committed. Resolved stack: `tauri =2.10.3` (Cargo.lock), `@tauri-apps/api 2.10.1`, `@tauri-apps/cli 2.10.1`, toolchain `stable-x86_64-pc-windows-msvc` (rustc 1.98.0) — the same MSVC toolchain SayIt pins. Environment findings for the implementation slice: the machine's rustup default is the GNU host, under which `windows-*` crates fail with `dlltool.exe: program not found`, so the harness pins MSVC exactly like `client/src-tauri/rust-toolchain.toml`; and a fresh lock resolution drifts past the accepted stack (`tauri-runtime-wry 2.11.4` breaks `tauri 2.10.3` with an E0308 signature change), so the harness lock pins the Tauri family to the accepted versions (`tauri-runtime`/`tauri-runtime-wry 2.10.1`, `tauri-utils 2.8.3`, `tauri-build 2.5.6`, `tauri-codegen`/`tauri-macros 2.5.5`, `wry 0.54.4`, `muda 0.17.1`, `tao 0.34.8`). Per the spike task, this PASS upgrades the contract's PCM-transfer condition to MATCH on PM acceptance; external WAV ingress and UI implementation remain locked until PM independently reviews the runtime evidence.

Z2 PM acceptance (2026-08-29): commit `d1f7c1fa6943e556d0957ec1bf7c73823193ba81` accepted. PM confirmed the isolated scope and clean diff, reran `npm ci`, Vitest 8/8, Vite build, Rust 4/4 tests, and `npm run spike:build` successfully, independently recomputed the deterministic payload hash/sentinels, verified the resolved Tauri versions, and launched the fresh packaged Windows WebView2 executable. The native panel passed with a 115 ms invoke, exact 9,438,418-byte `ArrayBuffer`, matching SHA-256 `ebf783be13a56bd212a474803c6b8d6da391b10bc0cfc5721f9422c90c045750`, sentinels 7/190/86, and zero fallback warnings. PM rebuild executable SHA-256: `0fdf6bb7789885affca9257c91668a1dfb38f130c9d023c66d2b52706852219f` (not committed; debug rebuild hashes are not expected to be reproducible). Contract upgraded to **MATCH**; only the PC external-ingress slice is unlocked.

Z3 external WAV ingress (Colleague Z, 2026-08-29, stage 7, base `63fcd92f36dd258e8e5a93b2b78d1f9c85dc8ca2`): implemented per the frozen MATCH contract and submitted for PM review. Rust: new `watch_receiver/admission.rs` (`AdmissionGate` `Idle`/`Reserved{request_id, reserved_at}`, request-correlated bounded oneshots — admission 5 s, run-start ack 10 s — Rust-owned lazy 300 s lease, stale-ID no-ops, idempotent request-conditional abort, `watch_gate_state()`, and `watch_read_reserved_pcm` header-stripping over the Z2-proven binary IPC); `server.rs` inserts admission after WAV validation and BEFORE the durable save (409 busy/not-ready/already-reserved/bridge-timeout never touch disk and preserve the previous accepted file; 500 for capture/preparation failures; `watch://audio-ready` + bounded `watch_run_started` acknowledgement precede `201`); `main.rs` gains only debug-only wiring (event sink in `.setup`, five `#[cfg(debug_assertions)]` handler entries, and a `#[path]` module mount that keeps `watch_receiver/mod.rs` byte-identical). TS: new `watchIngress.ts` (fail-closed adapter + boot reconciliation), additive `RecorderOrchestrator` (Phase A sync reserve + one-shot in-process capture; Phase B bounded native capture; `beginExternalRun` = validate -> per-run `connect(buildProviderCallbacks())` -> checked `start` with `disableAi: true` and no system prompt -> ack -> exact-accounting feed with short final chunk and pre-`stop` equality assertions -> sample-derived durations -> shared `finalizeRecording`; correlated `abortExternalRun`; PTT down/up reservation guards; `finishRun` gate release on every terminal path), thin `recorder.ts` delegation. Evidence: `cargo test watch_receiver` 34/34 (0) — 12 gate unit tests + 7 receiver integration tests (busy-409 preserves previous file; prepare-failure 500 writes nothing; bridge-timeout 409; 201 only after ack; ack-timeout 500 + release; save-failure 500 + release + run-abort) + preserved WAV/1A regressions; full `cargo test` 156 passed / 0 failed / 4 ignored (0); focused Vitest 28/28 (0) and full client Vitest 31 files 0 failures (0); `tsc && vite build` (0); `cargo build --release` (0) with the release-marker scan all-absent (the five 1A markers plus all five `watch_*` command names and `AdmissionGate`). `git diff --check` clean; only the allowed files changed; no token, WAV, executable, build cache, model, APK, or local path committed. Not VERIFIED end-to-end: stage 8 stays locked until PM independently reviews this slice.

Z3 Repair 1 + Watch UI candidate (Colleague Z, 2026-08-29, base `5da1a32279b372810d83504aca2021b0c8146763`): Part A fixed all three NO-GO blockers strictly before Part B. Repair 1 — event payloads are `serde_json::Value` objects with the frozen fields (Rust object/string assertions + TS string-payload fail-closed regression). Repair 2 — `begin_admission` (waiter registered) -> emit -> `wait_admission`, mirrored by `begin_run_ack`/`wait_run_ack` before the audio-ready emit; synchronous in-emit resolve regression accepted without stale/timeout. Repair 3 — external-run Provider errors skip the mic-only teardown (`stopCapture` 0 calls in the external regression; gate-release invoke exactly once; single History entry; no Paste) while the mic error path is unchanged (stopCapture regression green). Part B — Watch 0.2.0-dev.2 (`versionCode = 2`): three screens + inline upload states, masked token with explicit reveal, sample-derived duration, Stop auto-upload, Retry/Later with retained WAV, Pending-upload badge, explicit discard prompt, brief transport-only "Uploaded to PC", app-driven keep-screen-on, four haptics, no carousel dots, 480x480 round safe area. Design evidence frozen: `design/watch-ui/0.2.0-dev.1-baseline` (accepted-parent, parent SHA + resource hashes) and `design/watch-ui/0.2.0-dev.2-candidate.1` (candidate README, seven 480x480 state previews, SHA256SUMS); declared deviation — previews are layout renders, photographic screenshots deferred to the PM/user device session. Verification: client Vitest 31 files / 370 passed (0), `npm run build` (0), `cargo test watch_receiver` 36/36 (0), full `cargo test` 158/0/4 (0), `cargo build --release` (0) with all eleven release markers absent, `gradlew testDebugUnitTest lintDebug assembleDebug assembleRelease` successful (0) with debug APK SHA-256 `77e614cf2ce8accdda61a8221c0501d9210bd1a976b1425985835c7ae1625cb5` (not committed). `git diff --check` clean. Not VERIFIED end-to-end; no real device this round; stage 8 locked pending PM re-review.

Z3 Repair 1 PM review (2026-08-30, target `b8daef527acbdced401e73ec9be6eaee39373389`): **split decision**. PC Part A is retained: PM source review confirmed object-valued Tauri payloads, register-before-emit for admission and run ack, and external Provider-error teardown without mic `stopCapture`; fresh client Vitest 370/370 and TypeScript/Vite build passed. The current Rust test executable was independently run (158 passed / 0 failed / 4 ignored) and the existing Release exe contained none of the eleven Watch/admission markers. A fresh Cargo rebuild was not obtained in this PM run because the external `transcribe-cpp-sys` CMake/MSBuild cache failed with FTK1011/MSB3491 tracking-log errors; this is recorded as an environment verification limitation and must be rerun by Z before the next gate. Watch tests were forced to rerun (57 passed / 0 failed), lint plus Debug/Release APK builds passed, and Debug APK SHA-256 matched `77E614CF…25CB5`.

Watch UI `0.2.0-dev.2-candidate.1` is **NO-GO / Repair 2 required**. Actionable findings: (1) Cancel resets the Session before the I/O coroutine completes, so the late `recordingCompleted` assertion can turn the promised Ready state into Failure; (2) after Later, Pending upload is only a non-clickable badge and Retry is unreachable; (3) hand-authored previews show wide pill actions while runtime uses Wear Compose 1.4.1 default circular 52 dp `Button` for long labels, so previews are not faithful and controls can clip; (4) `uploading_body` tells the user to keep the watch on despite app-driven keep-screen-on; (5) the claimed parent baseline manifest is invalid — its `build.gradle.kts` hash matches the current dev.2 file rather than parent dev.1, and no parent screenshot exists. Candidate.1 and the invalid baseline stay immutable for audit. Repair instructions: `docs/DELIVERY-1B-Z3-REPAIR-2.md`.
