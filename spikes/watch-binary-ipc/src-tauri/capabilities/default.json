{
  "identifier": "default",
  "description": "Default capability for the isolated spike window; application commands need no ACL entries.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
